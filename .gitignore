# Build artifacts
/target
# Nested build trees too, not just the workspace root: 816 artifact files from
# mbus-core/panic-probe/target were committed because `/target` anchors to the repo
# root and matched none of them.
target/

# Coverage files
*.profraw
*.profdata
lcov.info
coverage/

# IDE and editor files
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*.rs.bak
*~

# Test artifacts
test_*.rs
/tmp/

# Pre-commit hooks
pre-commit.sh

# OpenCode database
.opencode/

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini

# AI/Claude tools
.claude/

# Cross-compilation outputs
arm/
aarch64/
cross/

# Generated documentation
/target/doc/

# Cache directories
.cache/
Cargo.lock~  # Backup lockfiles


# Secrets — AES key material must never be committed (see tools/oracle/README.md)
*.keys
*key*.json
keys.jsonl
keys/
*.local.hex
# Local device stores
*.redb
